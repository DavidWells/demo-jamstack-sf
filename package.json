{
  "name": "my-awesome-site",
  "private": true,
  "version": "0.0.0",
  "description": "Much wow, awesome!",
  "scripts": {
    "build": "mkdir -p build && cp src/index.html build",
    "demo": "cp -R ./plugins/serverless/backend backend"
  },
  "author": "@DavidWells",
  "license": "MIT",
  "devDependencies": {
    "@netlify/plugin-sitemap": "0.0.3",
    "netlify-plugin-debug-cache": "^1.0.0"
  }
}
